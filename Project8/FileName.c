#include <stdio.h>
int main()
{
	int 스트라이크, 아웃, 베이스 = 0;
	int a, b,d;
		printf("숫자야구");
		printf("\n");
			for (a=1; a<=9; a++)
			{
				printf("라운드:%d\n", a);
				scanf("%d", &d);
				return 0;
			}
}